import { TITLES } from '../../../config'
import MarkdownPage from '../markdown-page'

import content from './content.md'

const CookiePolicy = () => <MarkdownPage title={TITLES.COOKIE_POLICY} content={content} />

export default CookiePolicy
